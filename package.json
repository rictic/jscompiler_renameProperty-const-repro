{
    "name": "lit_element_repro",
    "version": "0.1.0",
    "private": true,
    "devDependencies": {
        "google-closure-compiler": "^20220803.0.0"
    },
    "scripts": {
        "build-advanced": "google-closure-compiler --js=main.js --compilation_level=ADVANCED",
        "build-simple": "google-closure-compiler --js=main.js --compilation_level=SIMPLE"
    },
    "dependencies": {
    }
}
